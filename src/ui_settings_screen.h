#pragma once
#include <lvgl.h>

// Экран «Настройки» тач-интерфейса — пороги и тайминги алгоритма осушения
// (целевая влажность, гистерезис, защита от замерзания, min-runtime/pause),
// то же самое, что форма #settingsForm в data/index.html. Значения
// редактируются степперами (+/-), а не текстовым вводом — на маленьком
// экране это надёжнее, чем попадать пальцем в точку на клавиатуре.
namespace UiSettingsScreen {

void build(lv_obj_t* parent);

// Перечитывает текущие RuntimeSettings и обновляет степперы. Вызывать
// только при открытии вкладки (не по таймеру!) — иначе можно затереть
// значение, которое пользователь ещё не успел сохранить.
void refresh();

}  // namespace UiSettingsScreen
