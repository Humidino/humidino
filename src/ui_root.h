#pragma once

// Корневой экран тач-интерфейса: 3 полноэкранных вида (Главная/Настройки/
// Пресеты), между которыми переключает нижняя панель навигации —
// плюс общая экранная клавиатура. Вызывается из display.cpp (lvglTask)
// вместо прежнего прямого UiDashboard::build()/update() на весь экран.
namespace UiRoot {

void build();
void update();  // вызывается по таймеру; сам решает, что из этого обновлять

}  // namespace UiRoot
