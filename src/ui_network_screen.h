#pragma once
#include <lvgl.h>

// Экран «Сеть» тач-интерфейса — настройки MQTT-брокера (хост, порт, логин,
// пароль, базовый топик), то же самое, что форма #networkForm в
// data/index.html. Wi-Fi сюда намеренно не входит — подключение к домашней
// сети остаётся за WiFiManager captive portal (см. README, п. 4), это
// отдельный процесс первого включения, а не то, что стоит дублировать на
// маленьком экране без физической клавиатуры.
namespace UiNetworkScreen {

void build(lv_obj_t* parent);

// Перечитывает сохранённые настройки MQTT из NVS и обновляет поля (пароль
// в поле никогда не подставляется — только индикатор "задан/не задан", как
// и в вебе). Вызывать при открытии вкладки, а не по таймеру.
void refresh();

}  // namespace UiNetworkScreen
