#pragma once
#include <lvgl.h>

// Экран "Статистика" (4-я вкладка тачскрина) — сводка (запусков сегодня/
// всего, время работы сегодня) и список последних запусков реле с
// показаниями датчиков на старте/остановке каждого цикла. Источник данных —
// журнал в run_log.h; в отличие от Main экран не обновляется каждый тик, а
// только при показе вкладки (см. UiRoot::showTab()) — это чтение с флеша,
// а не повод дёргать его на каждый кадр.
namespace UiStatsScreen {

void build(lv_obj_t* parent);
void refresh();

}  // namespace UiStatsScreen
