#pragma once
#include <cstdint>

#include "shared_state.h"

// Управление вентиляцией: читает состояние датчиков, управляет SSR-реле
// через приоритетную для безопасности state machine (защиты от конденсата и
// замерзания перекрывают минимальное время работы, которое защищает только
// ресурс реле, а не физическое оборудование). Работает как controlTask на
// ядре 0, с наивысшим приоритетом среди задач приложения — как единственная
// задача, влияющая на безопасность.
namespace Relay {

void begin();

// Текст баннера для данного состояния, используется в ui_dashboard.cpp.
// "Idle" и "MinPauseHold" оба выводят "ОЖИДАНИЕ" (по сути оба состояния —
// ожидание, у MinPauseHold просто известна причина).
const char* bannerText(RelayControlState state);

// Бейдж режима управления (АВТО/РУЧНОЕ ВКЛ/РУЧНОЕ ВЫКЛ) — отдельно от
// bannerText, т.к. экран без тачскрина показывает и режим, и текущий статус
// одновременно (переключить режим можно только из веб-интерфейса).
const char* modeBadgeText(OperatingMode mode);

}  // namespace Relay
