#pragma once
#include <lvgl.h>

// Экран «Пресеты» тач-интерфейса — список сохранённых наборов
// порогов/таймингов с кнопками "Применить"/"Удалить" на каждой строке и
// формой сохранения текущих настроек под новым (или существующим) именем.
// Дублирует то, что в data/index.html делают #presetList/#saveAsPresetBtn.
namespace UiPresetsScreen {

void build(lv_obj_t* parent);

// Перечитывает список пресетов из NVS и перерисовывает экран (возвращает
// его к виду списка, если была открыта форма ввода имени). Вызывать при
// открытии вкладки, а не по таймеру.
void refresh();

}  // namespace UiPresetsScreen
