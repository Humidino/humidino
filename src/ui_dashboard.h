#pragma once
#include <lvgl.h>

// Строит и обновляет главный экран дашборда: строка статуса (время
// работы, WiFi, ОЗУ, режим), три кнопки переключения режима (дублируют
// веб-интерфейс), сетка 2x2 из панелей зон и цветной баннер статуса,
// зависящий от RelayControlState. build() кладёт всё это в переданный
// контейнер (вкладку "Главная" из ui_root.cpp), а не прямо на экран —
// один физический экран теперь общий на несколько вкладок.
namespace UiDashboard {

void build(lv_obj_t* parent);
void update();  // читает актуальный SystemState и обновляет все подписи

}  // namespace UiDashboard
